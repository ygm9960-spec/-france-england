# v0.14 배경 경로 수정 및 연출 정리

## 확인한 원인
- CSS 사용자 변수에 들어간 상대 이미지 주소가 css/ 기준으로 해석됨.
  실제 파일은 images/에 있지만 요청은 css/images/로 향하므로 파일 존재 검사로는 발견할 수 없었음.
- 캐릭터 canvas 종횡비가 달라 object-fit:contain과 퍼센트 폭으로 얼굴 크기를 통일할 수 없었음.
- 누적 스타일의 변형·최소높이·최대높이가 장면 타입마다 충돌했음.

## 수정
- CSS 이미지 URL을 document.baseURI 기준 절대 URL로 해석. 하위 폴더 배포도 지원.
- 얼굴·눈 위치 추정값을 별도 portraitLayout.js로 분리. 동일 장면 인물에 공통 얼굴 높이 적용.
  일부 조연은 공통 추정값이므로 실기기 시각 검증 후 추가 미세 조정이 필요함.
- 배경은 화면 하단까지 유지하고 고정 대사창을 위에 표시. 반복 확대와 흔들림 제거.
- 중앙 나레이션은 SCENE 1~3만 유지. 나머지는 일반 대사창.
- 세 명의 잭 v0.28 실행 코드의 40/70/105ms 리듬을 참고:
  일반 32ms, 핵심 40ms, 쉼표 70ms, 마침표 105ms.
- 헛기침·의회 소집·일상 반응의 화면 충격 삭제.
  꿈 전환·회상 특수 이미지·최종 역논파·마지막 거울은 유지.
- 표정 로딩 중 기존 인물이 거의 투명해지던 처리 삭제.
- 교사용 논쟁 바로가기와 이어하기에서 캐릭터 이미지 연결 누락 수정.
- 기묘사화는 UIUX 설계서만 확보했으며 실행 파일은 비교하지 못했음.

## 검증 범위와 한계
- JS 문법, 원본 스토리/논쟁 데이터 동일성, 40개 장면 URL HTTP 응답, 얼굴 크기 계산 검사.
- Chromium 설치 다운로드 실패로 브라우저 스크린샷/전체 자동 플레이는 미실시.
  실기기 검증 완료본으로 간주하지 말 것.

## 배포
- ZIP을 풀어 index.html, css/, js/, images/, sw.js를 함께 같은 위치에 업로드.
- 이전 캐시가 남으면 제목 5회 탭 → 캐시 지우고 최신 버전 불러오기.
