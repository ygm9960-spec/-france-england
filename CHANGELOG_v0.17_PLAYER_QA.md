# PLAYER QA 보정 패치

현재 GitHub Pages 배포본(v0.16)의 실제 소스와 40개 장면 진행 흐름을 플레이어 관점에서 다시 순서대로 점검해 보정한 패치입니다. BGM/SFX와 개별 캐릭터 수동 위치 조정은 건드리지 않았습니다.

## 수정 내용

- 시작 포스터 위에 게임 제목 `태양왕이 여왕이 되었다`가 실제로 보이도록 복구했습니다. 제목 5회 탭 영역도 그대로 유지됩니다.
- 프롤로그 첫 문장이 화면에 보이지 않는 몽타주를 설명하던 문제를 고쳤습니다.
  - `17세기 후반, 프랑스 베르사유 궁전.` → 태양왕 소개 → 관료제 소개 순서로 시작합니다.
  - 검은 화면만 보여주지 않고 어둡게 처리한 베르사유 배경 위로 오프닝 문장이 나타납니다.
- 베르사유 궁정에서 `주변 의원들 / 멋적은`으로 표시되던 문장을 `주변 신하들 / 멋쩍은`으로 수정했습니다.
- 몸 교환 직전의 `dark_pulse`가 현재 빌드에서 실제 연출되지 않던 문제를 `flash_white`로 바꿔 장면 전환이 보이도록 했습니다.
- DEBATE I 직전에 게임 제목이 다시 뜨던 중복 타이틀을 `DEBATE I · 세금 / 의회와 왕권`으로 변경했습니다.
- `아이랜드` → `아일랜드`를 수정했습니다.
- 두 번째 의회 소집 직전의 단독 `좋아.`를 `그럼 답은 간단하군.`으로 바꿔 다음 대사와 자연스럽게 연결했습니다.
- 세 번째 논쟁 뒤 `애송이 앤은… → 됐다.`를 `애송이 앤은… → …아니다. 됐다.`로 다듬었습니다.
- 귀환 장면에서 몸 교환은 전날 밤 침실에서 시작했는데 왕좌실의 이전 순간으로 되돌아온 것처럼 보이던 시간·장소 모순을 수정했습니다.
  - 베르사유 침실에서 자신의 몸으로 돌아옴 → 보고서를 들고 왕좌실로 이동하는 짧은 브리지 나레이션으로 연결합니다.
- 일반적인 FRANCE / ENGLAND / MEMORY 장면 이동마다 국가명 카드가 반복되던 것을 조용한 암전 컷으로 낮췄습니다.
  - FINAL / REALIZATION / EPILOGUE 같은 실제 구조 전환은 기존 타이틀을 유지합니다.
- Service Worker 캐시 키를 갱신해 GitHub Pages에서 이전 CSS/JS가 남는 가능성을 줄였습니다.

## 유지한 것

- 40개 장면 구조
- 세 번의 논쟁과 6개 논리탄환
- FINAL 역논파 구조
- 이미지/인물 배치 계산
- 교사용 제목 5회 탭 진입
- 오디오 구조 및 BGM/SFX 상태
