# v0.19 PLAYER FLOW · 가로화면 / 연출 패치

이번 패치는 플레이어 체감 흐름을 우선으로 정리했다. BGM/SFX와 장면별 수동 이미지 좌표 조정은 건드리지 않았다.

- 프롤로그 첫 3개 나레이션은 타이핑을 제거했다. 문장이 한 번에 나타나고 잠시 유지된 뒤 자동으로 사라지며, 이 구간에서는 플레이어 터치로 넘길 수 없다.
- 프롱드의 기억 이미지는 잠깐 뜨는 컷이 아니라 SCENE 02가 끝날 때까지 유지한다. 원본 전체가 보이도록 `contain` 방식으로 표시해 아래쪽 잘림을 없앴다.
- 대사창뿐 아니라 배경·인물 등 스토리 화면의 빈 영역을 터치해도 대사를 진행할 수 있게 했다. 메뉴/팝업/특수컷 등 자체 조작이 있는 영역은 제외한다.
- `이야기 시작`/`이어하기`를 누르는 순간 브라우저가 허용하는 범위에서 즉시 전체화면을 요청하고 가로 방향 잠금을 시도한다. 브라우저 보안상 사용자 입력 없이 URL 바를 강제로 숨길 수 없으므로 시작 버튼 탭이 가능한 가장 빠른 전체화면 진입 시점이다.
- 20:9 같은 초광폭 가로 화면에서도 이야기 영역을 약 16:9 중앙 캔버스로 제한하고 좌우는 검은 레터박스로 처리했다. 림버스컴퍼니처럼 시선 이동 거리를 줄이는 방향이다.
- 2인 장면은 좌우 인물을 화면 중앙 쪽으로 더 모으고, 3인 장면도 중앙 집중형 자동 배치를 사용하도록 `portraitFrames`를 런타임에서 보정한다.
- 대사창 폭도 화면 끝까지 늘어나지 않게 좁혀 한 문장을 한눈에 읽기 쉽게 조정했다.
- 앤 여왕 즉위 보고서는 SCENE 04의 관련 대화가 끝날 때까지 오른쪽 상단에 고정해 유지한다.
- `monologue` 타입 대사는 화면 중앙의 별도 독백 영역으로 표시한다.
- `???` 대사는 배경/인물/HUD를 감춘 검은 화면 중앙에 붉은 글자로 표시한다. 기존 타이핑 효과는 그대로 유지한다.
- v0.17~v0.18의 시작화면 제목 복구, 개념 팝업 터치 진행, 일반 장면전환 약화, 플레이어 흐름 수정도 모두 포함한 누적 패치다.

## 적용 파일

- `css/presentation.css`
- `js/visualMap.js`
- `sw.js`

세 파일을 저장소의 같은 경로에 덮어쓰면 된다.
